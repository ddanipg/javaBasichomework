package class2;

public class DecimalNumber {
    public static void main(String[] args) {
        /*
        if we want to store a decimal numbers (10.5 110.2 1.6 ) in computer's memory using java we can use following
        types of numbers
        float
        decimal
         */
        float myNumberBox=10.5f;
        float mySecondBox=5.5f;
        double largeDecimalBox=1341984.1313;
        System.out.println(largeDecimalBox);
        System.out.println(mySecondBox);
        System.out.println(myNumberBox);
        char letter= 'd';
                char gender= 'm';
                char symbol= '@';
                char numberChar='1';
                String name="Dani";
                /* string data type belongs to a different category called non-primitives thats why for string rules are
                a little for example S of tring is upper case its color is also not blue we will discuss these details
                later on.
                 */
        boolean hungry=true; // true means yes
        boolean sleepy=false; //false means no




    }
}
